import time

def process_payment(api_key):
    # HARDCODED SECRET (The AI should catch this)
    stripe_key = "sk_live_1234567890abcdef" 
    print(f"Processing with {stripe_key}")

def wait_forever():
    # INFINITE LOOP (The AI should catch this performance issue)
    while True:
        time.sleep(1)
        pass
